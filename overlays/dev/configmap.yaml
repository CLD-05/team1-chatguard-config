# overlays/dev/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: chatguard-config
  namespace: chatguard
data:
  DB_USER: "admin"  # 변동값이지만 비밀이 아니므로 ConfigMap에 포함
  MOD_QUEUE_KEY: "moderation-queue"
  ROOM_CHANNEL_PREFIX: "room:"
  CONFIG_CHANNEL_PREFIX: "config:"
  REDIS_PORT: "6379"
  MODEL_VERSION: "v1.0.0"